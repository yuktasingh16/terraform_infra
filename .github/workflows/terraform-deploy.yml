name: Terraform Deploy

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "terraform/foundation/**"
      - "terraform/platform/**"
      - "terraform/database/**"
      - "terraform/modules/network/**"
      - "terraform/modules/iam/**"
      - "terraform/modules/ecr/**"
      - "terraform/modules/eks/**"
      - "terraform/modules/nodegroup/**"
      - "terraform/modules/rds/**"
      - ".github/workflows/terraform-deploy.yml"

permissions:
  contents: read
  id-token: write

env:
  AWS_REGION: us-east-1
  TERRAFORM_VERSION: 1.15.7
  TF_IN_AUTOMATION: true
  TF_INPUT: false
  TF_PLUGIN_CACHE_DIR: /home/runner/.terraform.d/plugin-cache

concurrency:
  group: terraform-deploy-main
  cancel-in-progress: false

jobs:
  foundation:
    name: Deploy Foundation
    runs-on: ubuntu-latest
    environment: terraform-foundation

    env:
      TF_WORKING_DIR: terraform/foundation

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}
          terraform_wrapper: false

      - name: Create Terraform plugin cache directory
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "${TF_PLUGIN_CACHE_DIR}"

      - name: Cache Terraform providers
        uses: actions/cache@v4
        with:
          path: ${{ env.TF_PLUGIN_CACHE_DIR }}
          key: ${{ runner.os }}-terraform-${{ env.TERRAFORM_VERSION }}-foundation-${{ hashFiles('terraform/foundation/.terraform.lock.hcl') }}
          restore-keys: |
            ${{ runner.os }}-terraform-${{ env.TERRAFORM_VERSION }}-foundation-
            ${{ runner.os }}-terraform-${{ env.TERRAFORM_VERSION }}-

      - name: Terraform format check
        shell: bash
        run: |
          set -euo pipefail

          terraform fmt -check -recursive terraform/foundation
          terraform fmt -check -recursive terraform/modules/network
          terraform fmt -check -recursive terraform/modules/iam
          terraform fmt -check -recursive terraform/modules/ecr

      - name: Verify AWS role secret is configured
        shell: bash
        env:
          AWS_ROLE_ARN_CHECK: ${{ secrets.AWS_ROLE_ARN }}
        run: |
          set -euo pipefail

          if [ -z "${AWS_ROLE_ARN_CHECK}" ]; then
            echo "::error::AWS_ROLE_ARN GitHub Actions secret is not configured."
            exit 1
          fi

          echo "AWS_ROLE_ARN secret is configured."

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ env.AWS_REGION }}
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          role-session-name: github-actions-foundation-deploy

      - name: Show AWS identity
        shell: bash
        run: |
          set -euo pipefail

          echo "=== AWS identity used by GitHub Actions ==="
          aws sts get-caller-identity

      - name: Verify Terraform backend access
        shell: bash
        run: |
          set -euo pipefail

          echo "=== Checking Terraform backend bucket ==="
          echo "Bucket: letstype-terraform-state"
          echo "Prefix: terraform/"

          if ! output=$(aws s3api head-bucket \
            --bucket letstype-terraform-state \
            --region "${AWS_REGION}" 2>&1)
          then
            echo "${output}"
            echo "::error::Cannot access Terraform state bucket. Confirm AWS_ROLE_ARN points to the IAM role with s3:ListBucket and s3:GetBucketLocation on arn:aws:s3:::letstype-terraform-state."
            exit 1
          fi

          if ! output=$(aws s3api list-objects-v2 \
            --bucket letstype-terraform-state \
            --prefix terraform/ \
            --max-items 1 \
            --region "${AWS_REGION}" \
            2>&1 >/dev/null)
          then
            echo "${output}"
            echo "::error::Cannot list Terraform state prefix. Confirm the IAM role allows s3:ListBucket for prefix terraform/ on arn:aws:s3:::letstype-terraform-state."
            exit 1
          fi

          echo "Terraform S3 backend access verified."

      - name: Diagnose Foundation state access
        shell: bash
        run: |
          set -euo pipefail

          echo "=== Checking Foundation state object ==="
          echo "Expected: s3://letstype-terraform-state/terraform/foundation.tfstate"

          set +e
          output=$(aws s3api head-object \
            --bucket letstype-terraform-state \
            --key terraform/foundation.tfstate \
            --region "${AWS_REGION}" \
            2>&1)
          status=$?
          set -e

          if [ "${status}" -eq 0 ]; then
            echo "Foundation state object exists and is accessible."
          else
            echo "${output}"

            if echo "${output}" | grep -Eq "403|Forbidden|AccessDenied"; then
              echo "::error::Foundation state object access is forbidden."
              echo "::error::The assumed IAM role needs access to terraform/foundation.tfstate."
              exit 1
            fi

            if echo "${output}" | grep -Eq "404|Not Found|NoSuchKey"; then
              echo "Foundation state does not exist yet."
              echo "This is acceptable for the first Foundation deployment."
              echo "Terraform apply will create the state object."
            else
              echo "::error::Unexpected error while checking Foundation state."
              exit 1
            fi
          fi

      - name: Terraform init
        working-directory: ${{ env.TF_WORKING_DIR }}
        shell: bash
        run: |
          set -euo pipefail
          terraform init -input=false

      - name: Terraform validate
        working-directory: ${{ env.TF_WORKING_DIR }}
        shell: bash
        run: |
          set -euo pipefail
          terraform validate -no-color

      - name: Terraform plan
        working-directory: ${{ env.TF_WORKING_DIR }}
        shell: bash
        run: |
          set -euo pipefail

          terraform plan \
            -input=false \
            -lock-timeout=5m \
            -no-color \
            -out=tfplan

      - name: Terraform apply
        working-directory: ${{ env.TF_WORKING_DIR }}
        shell: bash
        run: |
          set -euo pipefail

          terraform apply \
            -input=false \
            -lock-timeout=5m \
            -auto-approve \
            tfplan

      - name: Verify Foundation state after apply
        shell: bash
        run: |
          set -euo pipefail

          aws s3api head-object \
            --bucket letstype-terraform-state \
            --key terraform/foundation.tfstate \
            --region "${AWS_REGION}" \
            > /dev/null

          echo "Foundation state verified successfully."

  platform:
    name: Deploy Platform
    runs-on: ubuntu-latest
    environment: terraform-platform

    needs:
      - foundation

    env:
      TF_WORKING_DIR: terraform/platform

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}
          terraform_wrapper: false

      - name: Create Terraform plugin cache directory
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "${TF_PLUGIN_CACHE_DIR}"

      - name: Cache Terraform providers
        uses: actions/cache@v4
        with:
          path: ${{ env.TF_PLUGIN_CACHE_DIR }}
          key: ${{ runner.os }}-terraform-${{ env.TERRAFORM_VERSION }}-platform-${{ hashFiles('terraform/platform/.terraform.lock.hcl') }}
          restore-keys: |
            ${{ runner.os }}-terraform-${{ env.TERRAFORM_VERSION }}-platform-
            ${{ runner.os }}-terraform-${{ env.TERRAFORM_VERSION }}-

      - name: Terraform format check
        shell: bash
        run: |
          set -euo pipefail

          terraform fmt -check -recursive terraform/platform
          terraform fmt -check -recursive terraform/modules/eks
          terraform fmt -check -recursive terraform/modules/nodegroup

      - name: Verify AWS role secret is configured
        shell: bash
        env:
          AWS_ROLE_ARN_CHECK: ${{ secrets.AWS_ROLE_ARN }}
        run: |
          set -euo pipefail

          if [ -z "${AWS_ROLE_ARN_CHECK}" ]; then
            echo "::error::AWS_ROLE_ARN GitHub Actions secret is not configured."
            exit 1
          fi

          echo "AWS_ROLE_ARN secret is configured."

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ env.AWS_REGION }}
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          role-session-name: github-actions-platform-deploy

      - name: Show AWS identity
        shell: bash
        run: |
          set -euo pipefail

          echo "=== AWS identity used by GitHub Actions ==="
          aws sts get-caller-identity

      - name: Verify Terraform backend access
        shell: bash
        run: |
          set -euo pipefail

          echo "=== Checking Terraform backend bucket ==="
          echo "Bucket: letstype-terraform-state"
          echo "Prefix: terraform/"

          if ! output=$(aws s3api head-bucket \
            --bucket letstype-terraform-state \
            --region "${AWS_REGION}" 2>&1)
          then
            echo "${output}"
            echo "::error::Cannot access Terraform state bucket. Confirm AWS_ROLE_ARN points to the IAM role with s3:ListBucket and s3:GetBucketLocation on arn:aws:s3:::letstype-terraform-state."
            exit 1
          fi

          if ! output=$(aws s3api list-objects-v2 \
            --bucket letstype-terraform-state \
            --prefix terraform/ \
            --max-items 1 \
            --region "${AWS_REGION}" \
            2>&1 >/dev/null)
          then
            echo "${output}"
            echo "::error::Cannot list Terraform state prefix. Confirm the IAM role allows s3:ListBucket for prefix terraform/ on arn:aws:s3:::letstype-terraform-state."
            exit 1
          fi

          echo "Terraform S3 backend access verified."

      - name: Verify Foundation remote state exists
        shell: bash
        run: |
          set -euo pipefail

          echo "=== Verifying Foundation dependency ==="

          if ! aws s3api head-object \
            --bucket letstype-terraform-state \
            --key terraform/foundation.tfstate \
            --region "${AWS_REGION}" \
            > /dev/null
          then
            echo "::error::Foundation remote state is missing or inaccessible."
            echo "::error::Expected s3://letstype-terraform-state/terraform/foundation.tfstate"
            exit 1
          fi

          echo "Foundation remote state exists and is accessible."

      - name: Terraform init
        working-directory: ${{ env.TF_WORKING_DIR }}
        shell: bash
        run: |
          set -euo pipefail
          terraform init -input=false

      - name: Terraform validate
        working-directory: ${{ env.TF_WORKING_DIR }}
        shell: bash
        run: |
          set -euo pipefail
          terraform validate -no-color

      - name: Terraform plan
        working-directory: ${{ env.TF_WORKING_DIR }}
        shell: bash
        run: |
          set -euo pipefail

          terraform plan \
            -input=false \
            -lock-timeout=5m \
            -no-color \
            -out=tfplan

      - name: Terraform apply
        working-directory: ${{ env.TF_WORKING_DIR }}
        shell: bash
        run: |
          set -euo pipefail

          terraform apply \
            -input=false \
            -lock-timeout=5m \
            -auto-approve \
            tfplan

      - name: Verify Platform state after apply
        shell: bash
        run: |
          set -euo pipefail

          aws s3api head-object \
            --bucket letstype-terraform-state \
            --key terraform/platform.tfstate \
            --region "${AWS_REGION}" \
            > /dev/null

          echo "Platform state verified successfully."

  database:
    name: Deploy Database
    runs-on: ubuntu-latest
    environment: terraform-database

    needs:
      - platform

    env:
      TF_WORKING_DIR: terraform/database
      TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}
          terraform_wrapper: false

      - name: Create Terraform plugin cache directory
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "${TF_PLUGIN_CACHE_DIR}"

      - name: Cache Terraform providers
        uses: actions/cache@v4
        with:
          path: ${{ env.TF_PLUGIN_CACHE_DIR }}
          key: ${{ runner.os }}-terraform-${{ env.TERRAFORM_VERSION }}-database-${{ hashFiles('terraform/database/.terraform.lock.hcl') }}
          restore-keys: |
            ${{ runner.os }}-terraform-${{ env.TERRAFORM_VERSION }}-database-
            ${{ runner.os }}-terraform-${{ env.TERRAFORM_VERSION }}-

      - name: Terraform format check
        shell: bash
        run: |
          set -euo pipefail

          terraform fmt -check -recursive terraform/database
          terraform fmt -check -recursive terraform/modules/rds

      - name: Verify DB password secret is configured
        shell: bash
        env:
          DB_PASSWORD_CHECK: ${{ secrets.DB_PASSWORD }}
        run: |
          set -euo pipefail

          if [ -z "${DB_PASSWORD_CHECK}" ]; then
            echo "::error::DB_PASSWORD GitHub Actions secret is not configured."
            exit 1
          fi

          echo "DB_PASSWORD secret is configured."

      - name: Verify AWS role secret is configured
        shell: bash
        env:
          AWS_ROLE_ARN_CHECK: ${{ secrets.AWS_ROLE_ARN }}
        run: |
          set -euo pipefail

          if [ -z "${AWS_ROLE_ARN_CHECK}" ]; then
            echo "::error::AWS_ROLE_ARN GitHub Actions secret is not configured."
            exit 1
          fi

          echo "AWS_ROLE_ARN secret is configured."

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ env.AWS_REGION }}
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          role-session-name: github-actions-database-deploy

      - name: Show AWS identity
        shell: bash
        run: |
          set -euo pipefail

          echo "=== AWS identity used by GitHub Actions ==="
          aws sts get-caller-identity

      - name: Verify Terraform backend access
        shell: bash
        run: |
          set -euo pipefail

          echo "=== Checking Terraform backend bucket ==="
          echo "Bucket: letstype-terraform-state"
          echo "Prefix: terraform/"

          if ! output=$(aws s3api head-bucket \
            --bucket letstype-terraform-state \
            --region "${AWS_REGION}" 2>&1)
          then
            echo "${output}"
            echo "::error::Cannot access Terraform state bucket. Confirm AWS_ROLE_ARN points to the IAM role with s3:ListBucket and s3:GetBucketLocation on arn:aws:s3:::letstype-terraform-state."
            exit 1
          fi

          if ! output=$(aws s3api list-objects-v2 \
            --bucket letstype-terraform-state \
            --prefix terraform/ \
            --max-items 1 \
            --region "${AWS_REGION}" \
            2>&1 >/dev/null)
          then
            echo "${output}"
            echo "::error::Cannot list Terraform state prefix. Confirm the IAM role allows s3:ListBucket for prefix terraform/ on arn:aws:s3:::letstype-terraform-state."
            exit 1
          fi

          echo "Terraform S3 backend access verified."

      - name: Verify Foundation remote state exists
        shell: bash
        run: |
          set -euo pipefail

          if ! aws s3api head-object \
            --bucket letstype-terraform-state \
            --key terraform/foundation.tfstate \
            --region "${AWS_REGION}" \
            > /dev/null
          then
            echo "::error::Foundation remote state is missing or inaccessible."
            echo "::error::Expected s3://letstype-terraform-state/terraform/foundation.tfstate"
            exit 1
          fi

          echo "Foundation remote state exists and is accessible."

      - name: Verify Platform remote state exists
        shell: bash
        run: |
          set -euo pipefail

          if ! aws s3api head-object \
            --bucket letstype-terraform-state \
            --key terraform/platform.tfstate \
            --region "${AWS_REGION}" \
            > /dev/null
          then
            echo "::error::Platform remote state is missing or inaccessible."
            echo "::error::Expected s3://letstype-terraform-state/terraform/platform.tfstate"
            exit 1
          fi

          echo "Platform remote state exists and is accessible."

      - name: Terraform init
        working-directory: ${{ env.TF_WORKING_DIR }}
        shell: bash
        run: |
          set -euo pipefail
          terraform init -input=false

      - name: Terraform validate
        working-directory: ${{ env.TF_WORKING_DIR }}
        shell: bash
        run: |
          set -euo pipefail
          terraform validate -no-color

      - name: Terraform plan
        working-directory: ${{ env.TF_WORKING_DIR }}
        shell: bash
        run: |
          set -euo pipefail

          terraform plan \
            -input=false \
            -lock-timeout=5m \
            -no-color \
            -out=tfplan

      - name: Terraform apply
        working-directory: ${{ env.TF_WORKING_DIR }}
        shell: bash
        run: |
          set -euo pipefail

          terraform apply \
            -input=false \
            -lock-timeout=5m \
            -auto-approve \
            tfplan

      - name: Verify Database state after apply
        shell: bash
        run: |
          set -euo pipefail

          aws s3api head-object \
            --bucket letstype-terraform-state \
            --key terraform/database.tfstate \
            --region "${AWS_REGION}" \
            > /dev/null

          echo "Database state verified successfully."
